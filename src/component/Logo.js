import { Typography } from '@material-ui/core';
import React from 'react';

const Logo = (props) => {
  return <Typography variant='h1' style={{
    color: "#F5F5F5", textDecoration
      : "none"
  }}>TODO APP</Typography>;
};

export default Logo;
